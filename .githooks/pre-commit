#!/bin/sh
set -eu
root=$(git rev-parse --show-toplevel)
if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is required for publication checks; install Python and retry." >&2
  exit 1
fi
exec python3 "$root/scripts/check_publication.py" --root "$root"
